---
icon: bridge-circle-exclamation
---

# Errores nivel de puente

Estos errores ocurren durante el procesamiento de entradas en sistemas externos o "puentes" en la fase de "preparación" de la entrada.&#x20;

No tienen códigos de estado HTTP asociados ya que suceden en segundo plano cuando los puentes procesan las intenciones.

Para informar un error, un puente debe agregar una firma a la intención (incluida en la entrada) llamando a la API del libro mayor:

`POST /v2/intents/:handle/sign`

La firma debe incluir\
`custom.status = 'failed'` ,  \
`custom.reason` establecido a una de las razones en el grupo **bridge.**\* ,\
`custom.detail` con un mensaje que explique el error en detalle \
`custom.failid` (opcional pero recomendado) para hacer referencia al error en el sistema externo, típicamente un código de error o clase

Este campo `failId` es valioso para propósitos de diagnóstico.

Los puentes pueden informar **solo los errores** de este grupo. La API del libro mayor **rechazará** todos los demás errores.

<table><thead><tr><th width="129">Status</th><th>Reason</th><th>Detail</th></tr></thead><tbody><tr><td><code>N/A</code></td><td><code>bridge.account-not-found</code></td><td>La cuenta referenciada no existe en el sistema del puente.</td></tr><tr><td><code>N/A</code></td><td><code>bridge.account-inactive</code></td><td>La cuenta referenciada está inactiva en el sistema del puente, pero existe en el sistema.</td></tr><tr><td><code>N/A</code></td><td><code>bridge.account-insufficient-balance</code></td><td>Saldo insuficiente en la cuenta en el sistema del puente para realizar la transacción financiera especificada en la entrada.</td></tr><tr><td><code>N/A</code></td><td><code>bridge.account-limit-exceeded</code></td><td>La cuenta tiene saldo suficiente pero ya ha excedido el límite transaccional.</td></tr><tr><td><code>N/A</code></td><td><code>bridge.entry-rejected</code></td><td>La entrada fue rechazada para la cuenta referenciada. </td></tr><tr><td><code>N/A</code></td><td><code>bridge.entry-hash-invalid</code></td><td>El hash de la entrada recibida no es válido.</td></tr><tr><td><code>N/A</code></td><td><code>bridge.entry-signature-invalid</code></td><td>Alguna de las firmas de la entrada recibida no es válida. Se activa por la primera firma inválida.</td></tr><tr><td><code>N/A</code></td><td><code>bridge.entry-signature-mismatch</code></td><td>Todas las firmas de la entrada son válidas pero no hay una firma coincidente con la llave del libro mayor esperada.</td></tr><tr><td><code>N/A</code></td><td><code>bridge.entry-signature-missing</code></td><td>La entrada recibida no tiene ninguna firma.</td></tr><tr><td><code>N/A</code></td><td><code>bridge.entry-schema-invalid</code></td><td>La entrada recibida no coincide con el esquema esperado. Se devuelve en caso de que los campos raíz o de datos no coincidan con el esquema o que la intención incluida no coincida con el esquema de intención.</td></tr><tr><td><code>N/A</code></td><td><code>bridge.entry-mismatch</code></td><td>Los datos recibidos en la entrada no coinciden con la intención incluida en la entrada. En caso de que la fuente, el destino, el monto o el símbolo en la entrada no coincidan con la reclamación relacionada en la intención.</td></tr><tr><td><code>N/A</code></td><td><code>bridge.entry-duplicated</code></td><td>Ya se recibió anteriormente una entrada diferente para la misma reclamación de intención con la misma fuente o destino. Se utiliza para rechazar el doble gasto para la misma intención. <em>Este error no</em> <em>debe usarse al manejar la idempotencia.</em></td></tr><tr><td><code>N/A</code></td><td><code>bridge.entry-timeout</code></td><td>Ha pasado demasiado tiempo desde el inicio de la intención hasta la recepción de la entrada que incluye esta intención.</td></tr><tr><td><code>N/A</code></td><td><code>bridge.intent-hash-invalid</code></td><td>El hash de la intención en la entrada recibida no es válido.</td></tr><tr><td><code>N/A</code></td><td><code>bridge.intent-signature-invalid</code></td><td>Alguna de las firmas de la intención recibida en la entrada no es válida. Se activa por la primera firma inválida.</td></tr><tr><td><code>N/A</code></td><td><code>bridge.intent-signature-missing</code></td><td>La intención recibida en la entrada no tiene ninguna firma.</td></tr><tr><td><code>N/A</code></td><td><code>bridge.intent-unrelated</code></td><td>La intención recibida en la entrada no puede ser reconocida o correlacionada en el puente. Puede significar que no está firmada por la clave esperada o que la referencia de la intención no existe en el sistema del puente.</td></tr><tr><td><code>N/A</code></td><td><code>bridge.command-unsupported</code></td><td>El puente no admite el comando recibido. Por ejemplo, si el puente no puede procesar la anulación de un crédito, agregará una prueba a la intención con estado<code>error</code>y esta razón, y al hacerlo, suspenderá el proceso de anulación de la intención.</td></tr><tr><td><code>N/A</code></td><td><code>bridge.fraud-detected</code></td><td>La entrada fue marcada como fraude basándose en el análisis de la intención en el puente.</td></tr><tr><td><code>N/A</code></td><td><code>bridge.ledger-failed</code></td><td>Ocurrió un error al llamar a la API del libro mayor. Si el puente necesita realizar algunas llamadas a la API del libro mayor como precondición para firmar la intención. Se recomienda establecer el<code>failId</code>como la<code>reason</code>del error recibido del libro mayor en la firma que informa el error.</td></tr><tr><td><code>N/A</code></td><td><code>bridge.core-unreachable</code></td><td>No se puede acceder al sistema central (bancario) desde el puente. Generalmente si el sistema central está fuera de servicio por mantenimiento o tiene un tiempo de inactividad programado regular, lo cual a veces es un comportamiento normal de las instituciones financieras.</td></tr><tr><td><code>N/A</code></td><td><code>bridge.unexpected-core-error</code></td><td>Error genérico del sistema central (bancario). Si se recibe un error del sistema central que no se puede clasificar como ninguno de los errores de puente existentes. Se recomienda en la firma relacionada establecer el<code>failId</code>como el identificador de tipo de error interno del núcleo (código de error o nombre de clase o similar).</td></tr><tr><td><code>N/A</code></td><td><code>bridge.unexpected-error</code></td><td>Ocurrió un error inesperado en el puente al procesar la entrada. En caso de que ocurra un error en el proceso del puente que maneja la acción y no se pueda clasificar como ninguno de los errores de puente existentes. Se recomienda establecer el<code>failId</code>como el identificador de tipo de error (nombre de clase de error o similar) y el<code>detail</code>como el mensaje de error en la firma que informa el error.</td></tr><tr><td><code>N/A</code></td><td><code>bridge.incorrect-fee</code></td><td>El puente no pudo validar la tarifa de la transacción o es inválida.</td></tr><tr><td><code>N/A</code></td><td><code>bridge.proxy-response-invalid</code></td><td>La respuesta devuelta por el puente para una solicitud proxy no es válida. Esto puede ocurrir debido a la validación del<code>schema</code>.</td></tr></tbody></table>
